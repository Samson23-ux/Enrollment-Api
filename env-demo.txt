# development environment
ENVIRONMENT=development

# API DB 
API_DB=your_db_name
API_DB_USER=your_db_user
API_DB_PASSWORD=your_db_password
API_DB_PORT=your_api_db_port

# Database urls
ASYNC_DB_URL=postgresql+asyncpg://${your_db_user}:${your_db_password}@localhost:5432/${app_db_name}
SYNC_DB_URL=postgresql+psycopg2://${your_db_user}:${your_db_password}@localhost:5432/${app_db_name}
ASYNC_TEST_DB_URL=postgresql+asyncpg://${your_db_user}:${your_db_password}@localhost:5432/${app_test_db_name}

# RabbitMQ url
BROKER_URL=amqp://${your_broker_user}:${your_broker_password}@localhost:5672/${your_broker_vhost}

# Argon2
ARGON2_PEPPER=your_argon2_pepper

# Authentication
JWT_ALGORITHM=jwt_algorithm
ACCESS_TOKEN_SECRET_KEY=your_access_token_secret_key
ACCESS_TOKEN_EXPIRE_TIME=your_access_token_expire_time
REFRESH_TOKEN_SECRET_KEY=your_refresh_token_secret_key
REFRESH_TOKEN_EXPIRE_TIME=your_refresh_token_expire_time

# Admin
ADMIN_NAME=your_admin_name
ADMIN_EMAIL=your_admin_email
ADMIN_NATIONALITY=your_admin_nationality
ADMIN_PASSWORD=your_admin_password

- Use set admin deatils to perform admin related actions

# Sentry
SENTRY_SDK_DSN=your_sentry_dsn

- sign up/login and get your sentry_dsn at <insert sentry> for logging, observation and metrics